@page
@model ColorSet.Pages.Admin.wallColor.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>wallColor</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.wallColor.Color)
        </dt>
        <dd class="col-sm-10">
            <input style="width:200px" type="color" asp-for="@Model.wallColor.Color" class="form-control" />
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.wallColor.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
