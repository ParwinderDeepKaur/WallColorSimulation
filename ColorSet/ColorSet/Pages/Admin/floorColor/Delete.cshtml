@page
@model ColorSet.Pages.Admin.floorColor.DeleteModel

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>floorColor</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.floorColor.Color)
        </dt>
        <dd class="col-sm-10">
            <input style="width:250px" disabled="disabled" type="color" asp-for="@Model.floorColor.Color" class="form-control" />
        </dd>
    </dl>
    
    <form method="post">
        <input type="hidden" asp-for="floorColor.Id" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-page="./Index">Back to List</a>
    </form>
</div>
