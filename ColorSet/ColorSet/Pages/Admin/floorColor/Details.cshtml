@page
@model ColorSet.Pages.Admin.floorColor.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>floorColor</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.floorColor.Color)
        </dt>
        <dd class="col-sm-10">
            <input style="width:250px" disabled="disabled" type="color" asp-for="@Model.floorColor.Color" class="form-control" />
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.floorColor.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
